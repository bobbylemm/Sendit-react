import styled from 'styled-components';

const BackgroungImg = styled.main`
  flex: 1;
  background-position: center center;
  background-size: cover;
  overflow: auto;
`;

export default BackgroungImg;
